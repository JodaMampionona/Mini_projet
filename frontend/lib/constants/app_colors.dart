import 'dart:ui';

class AppColors {
  // main
  static const lightBg = Color(0xFFFFFDF4);

  // accents
  static const accentMain = Color(0xFFF7C945);
  static const accentSecondary = Color(0xFF2196F3);
  static const accentDark = Color(0xFF2F2E41);

  // selection
  static const textSelection = Color(0x61FFCE05);

  // text
  static const textMain = Color(0xFF27272B);
  static const textSecondary = Color(0xFF484850);
  static const placeholder = Color(0xC272787C);

  // inputs, cards, ...
  static const cardColor = Color(0xFFDDD6C3);
  static const inputBackground = Color(0xFFFAFAFA);
  static const listTileBackground = Color(0xFFF6F6F6);

  // status
  static const danger = Color(0xFFDC3545);
}
